### bitcoin.conf configuration file. Lines beginning with # are comments.
### File generated by dLightning Portal

## JSON-RPC options (for controlling a running bitcoin-qt/bitcoind process)

# server=1 tells Bitcoin to accept JSON-RPC commands.
server=1

# daemon=1 tells bitcoind to start in daemon mode
daemon=1
# You must set rpcuser and rpcpassword to secure the JSON-RPC api
# You should create your own new random password. 
# The username and password MUST NOT be the same.

rpcuser=dlightning
rpcpassword=dlightning

# How many seconds bitcoin will wait for a complete RPC HTTP request
# after the HTTP connection is established. 
rpctimeout=30

###Pruning adjusted to 25GB
prune=25000

### Lightning Node parameters
zmqpubrawblock=tcp://127.0.0.1:18501
zmqpubrawtx=tcp://127.0.0.1:18501
walletbroadcast=0
